pipeline{
  agent any
    stages {
      stage('Build apps') {
        steps{
          sh('mvn clean package')
        }
        post {
          success {
            echo "Archiving artifcats"
            archiveArtifacts artifacts: '**/*.war'
          }
        }
      }
   }
}
